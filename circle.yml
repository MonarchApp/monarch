machine:

  timezone:
    America/Los_Angeles

dependencies:
  override:
    - scripts/ci-setup
    - scripts/ci-build
  cache_directories:
    - ~/node_modules

test:
  override:
    - scripts/ci-test

deployment:
  development:
    branch: [develop, ci-integration]
    commands:
      - scripts/ci-deploy
